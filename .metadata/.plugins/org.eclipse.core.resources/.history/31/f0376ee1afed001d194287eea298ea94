.login-main{
	background:url("../images/equipment_main.jpg");
	background-attachment:fixed;
	background-position:center;
	background-repeat:no-repeat;
	background-size:cover;
	overflow:hidden;
	width:100vw;
	height:100vh;
	display:flex;
	justify-content:center;
	align-items:center;
}

.login-container-signin{
	width: 30%;
    padding: 30px;
    height: 50%;
    background-color: var(--white); 
	border-radius: var(--button-radius);
	display:flex;
	justify-content:center;
	align-items:center;
	position:relative;
}
.login-container-form{
	width:70%;
}
.login-container-form-title{
	text-align:center;
	margin-bottom:2rem;
}
.login-container-form-el{
	display:block;
	width:calc(100% - var(--inputOriginBorderSize) * 2 - var(--inputOrigiPaddingSize) * 2);
	height:30px;
	margin-bottom:1.5rem;
}
#login-state-message{
	height: 50px;
	text-align:center;
	font-size:10px;
	color:red;
}
.loginPage-btn{
	background-color: var(--blue);
	border-radius:var(--button-radius);
	border : none;
	height: 2rem;
	color: var(--white);
	font-size:14px;
	width: 100%;
	pointer-events: auto;
	padding:0;
	margin-bottom:2rem;
}
.signup-link{
	font-size:12px;
	position:absolute;
	bottom:20px;
	right:20px;
}
